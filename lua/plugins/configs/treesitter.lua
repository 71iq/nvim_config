local options = {
  ensure_installed = { "lua", "vim", "vimdoc", "bash", "cpp", "python", "v" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
